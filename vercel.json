{
  "installCommand": "npm install --legacy-peer-deps",
  "buildCommand": "vite build && esbuild server/_core/index.ts --platform=node --packages=external --bundle --format=esm --outdir=dist && esbuild server/_core/lambda.ts --platform=node --packages=external --bundle --format=esm --outfile=dist/lambda.js",
  "outputDirectory": "dist/public",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/[[...path]]" }
  ]
}