import { describe, expect, it } from "vitest";
import * as XLSX from "xlsx";
import { parseOperationMap } from "./operationMapParser";
describe("operationMapParser",()=>{it("classifica operações por departamento",()=>{const sheet=XLSX.utils.aoa_to_sheet([["Armazem","Empresa","UF","DEPTO"],["01-DISPONIVEL","0101","SP","PEÇAS"],["01-DISPONIVEL","0102","SP","OFICINAS"],["02-PROD","0105","INDÚSTRIA","IND - PEÇAS"],["31-LOC","0301","MG","IMPLEMENTOS"]]);const workbook=XLSX.utils.book_new();XLSX.utils.book_append_sheet(workbook,sheet,"Planilha1");const content=XLSX.write(workbook,{type:"buffer",bookType:"xlsx"});const result=parseOperationMap(content);expect(result.sourceRows).toBe(4);expect(result.rows).toHaveLength(4);expect(result.rows.find(row=>row.department==="PEÇAS")?.operation).toBe("AUTOPECAS");expect(result.rows.find(row=>row.department==="OFICINAS")?.operation).toBe("SERVICOS");expect(result.rows.find(row=>row.department==="IND - PEÇAS")?.operation).toBe("INDUSTRIA");expect(result.rows.find(row=>row.department==="IMPLEMENTOS")?.operation).toBe("IMPLEMENTOS");});});
